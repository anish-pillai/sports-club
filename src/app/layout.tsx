import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

import { Providers } from "@/components/providers/providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sports Arena Booking | Book Sports Venues & Coaching Classes",
  description: "Book sports arenas, courts, and coaching classes for various sports including basketball, tennis, football, and more. Find the perfect venue for your next game or training session.",
  keywords: ["sports booking", "arena booking", "court reservation", "sports coaching", "tennis courts", "basketball courts", "football fields"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
